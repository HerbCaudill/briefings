Select the strongest stories for today's briefing from this list of headlines. Prefer stories that are important, timely, and have multiple useful sources. Exclude sports, celebrity news, and thin evergreen features.

Return only valid JSON matching this TypeScript type:

```ts
type BriefingSelection = {
  stories: Array<{
    headline: string
    section: "World" | "US" | "Spain" | "Barcelona & Catalunya"
    sourceUrls: string[]
  }>
}
```
